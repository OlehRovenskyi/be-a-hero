#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: PSQL
#  config:
#    APP_NAME: users-management-workshop
setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production

#build:
#  docker:
#    web: Dockerfile
#  config:
#    NODE_ENV: production
#  run:
#    web: npm run start:prod